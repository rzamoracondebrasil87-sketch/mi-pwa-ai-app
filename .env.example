# Example environment variables
# Set GOOGLE_CLOUD_CREDENTIALS to a base64-encoded service account JSON
# e.g. on Windows PowerShell:
# $b64 = [Convert]::ToBase64String([IO.File]::ReadAllBytes('path\\to\\service-account.json'))
# setx GOOGLE_CLOUD_CREDENTIALS $b64

GOOGLE_CLOUD_CREDENTIALS=

# Optional: set NODE_ENV=production in deployment
NODE_ENV=development
# Gemini API Key
# Get your API key from: https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=your_api_key_here
